version: "3.9"

services:
  box-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: box-api
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    restart: unless-stopped
